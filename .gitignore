__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/
# exports written into the checkout by mistake
claude-session-exports/
.claude/launch.json
.preview/
# the ClickHouse connection string (see .env.example), and the lab's settings (lab/.env, see lab/.env.example)
.env
# the lab's API key for each instance (Metabase can't show a key again)
lab/state.json
