{
  "description": "When a session ends, share it to the team's ClickHouse if the rde skill ran in it and ~/.config/convo-analysis/.env has a CLICKHOUSE_URL (only this machine's rows change), and reload the local Postgres if it is running. Does nothing until one of them is set up; runs in the background.",
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/warehouse_hook.sh",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
